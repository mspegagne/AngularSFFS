'use strict';

/**
 * @ngdoc function
 * @name candyStoreApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the candyStoreApp
 */

 /**
 * A ajouter dans votre controleur
 
        dataService.getData().then(function (response) {
            $scope.products = response.data;
        });
		
**/